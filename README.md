# BNDR // MIX

## Run — zero chance of failure

```bash
npm ci
npm run dev
```

Open http://localhost:5173

That's it. `npm ci` installs exact locked versions — no resolution, no surprises.

## Requirements

Node 18+. Everything else is in package-lock.json.

## Build for production

```bash
npm run build
npm run preview
```
